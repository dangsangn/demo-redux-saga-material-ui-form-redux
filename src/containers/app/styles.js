const styles = (theme) => {
    return {
        box: {
            display: "flex",
            alignItems: "center",
            borderRadius: "4px",
        },
        shape: {
            padding: 10,
            margin: 10,
            backgroundColor: "#123456",
            color: "#fafafa",
            cursor: "pointer",
        },
    };
};

export default styles;
