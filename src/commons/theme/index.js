import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  color: {
    primary: "#4791db",
    secondary: "#e33371",
    error: "#f44336",
  },
  typography: {
    fontFamily: "Roboto",
  },
});

export default theme;
